# Optional. Enables cloud sync of personal notes on the live site.
# Create a GitHub classic token with the "gist" scope and set it in Vercel
# Environment Variables as SYNC_TOKEN, then redeploy.
# Alternatively connect a Vercel KV store (injects KV_REST_API_URL / KV_REST_API_TOKEN).
SYNC_TOKEN=
